# Supabase project (Auth + Postgres + Edge Functions). The URL below is the
# real project ref (rlizubjugevyxsfzmpny) — not a secret, safe as a
# placeholder default. NEVER put the actual anon key here.
VITE_SUPABASE_URL=https://rlizubjugevyxsfzmpny.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional override for Edge Functions base URL (e.g. local
# `supabase functions serve`). Defaults to `${VITE_SUPABASE_URL}/functions/v1`.
# VITE_SUPABASE_FUNCTIONS_URL=http://localhost:54321/functions/v1
