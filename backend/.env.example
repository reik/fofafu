# Server
PORT=4000
NODE_ENV=development

# Database
DB_PATH=./fofafu.db

# Auth
JWT_SECRET=change-me-to-a-real-secret
JWT_EXPIRES_IN=7d

# Email (Gmail SMTP with app password)
GMAIL_USER=
GMAIL_APP_PASSWORD=
FRONTEND_URL=http://localhost:5173

# Supabase (Phase 5 migration — see fofafu_vault/features/migrate-render-to-vercel-supabase.md)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
