{
  "name": "fofafu",
  "version": "0.1.0",
  "private": true,
  "description": "Foster Families community platform — rewrite driven by a multi-agent company structure with an Obsidian-vault source of truth.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm run test:run --workspace backend",
    "test:frontend": "npm run test --workspace frontend -- --run",
    "test:e2e": "npm run test:e2e --workspace frontend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend"
  }
}
