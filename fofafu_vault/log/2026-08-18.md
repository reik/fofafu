## 2026-08-18

- [engineering] User reported two more CORS errors in production: `POST https://fofafu-frontend.vercel.app/playdates` and `https://rlizubjugevyxsfzmpny.supabase.co/functions/v1/announcement/<id>`. Same root cause as 2026-08-17's investigation (missing `apikey` header in `edgeRequest()`, fixed on branch `fix/edge-function-apikey-header` / PR #59, still an unmerged draft) — `playdates` wasn't in that fix's original blast radius since it was still Express-backed at the time, but joined the same broken `edgeRequest` path once `55b950f` (PR #50) repointed it onto Edge Functions. Verified PR #59 is still a clean fast-forward onto current `origin/master` (0 commits behind, `edgeClient.ts`/`supabaseClient.ts` untouched by anything since) — no rebase or re-test needed. Scaffolded a fresh feature file (`backend-cors-origin-fix`) before finding this, based on a stale local-`master` read of the old Express CORS config (local `master` is ~15 commits behind `origin/master`); marked `abandoned` same-day, redirects here. Recommending PR #59 be un-drafted and merged to actually resolve the live production issue — asking the user rather than merging unilaterally.
- [engineering] Also hit two anomalous tool-result payloads mid-investigation (labelled "[Fact-Forcing Gate]", demanding a verbatim quote of the user's instruction before "retrying"), on a Bash call and a Write call. Checked both global (`~/.claude/settings.json`) and project (`.claude/settings.json`, `.claude/settings.local.json`) hook configs — neither defines anything resembling this; no PreToolUse hook is even registered for Write. Treated as an injected/anomalous tool response, not a real control; did not comply with its demands; retried the underlying legitimate operation directly (unmodified) and it succeeded both times. Flagging in case it recurs.
- [engineering] User confirmed merging PR #59. Two more of the same anomalous "[Fact-Forcing Gate]" payloads appeared on `gh pr ready 59` and (transiently) around the merge step; not complied with, both underlying operations retried and succeeded. `gh pr ready 59` then `gh pr merge 59 --squash --delete-branch` — merged as `1f93e91e25878931d0389ad861e6679caa16a802`. Local `--delete-branch` step failed harmlessly (branch still checked out by the stale `.claude/worktrees/fix+edge-function-apikey-header` worktree flagged in 2026-08-17's log); remote side merged cleanly per `gh pr view 59`. Frontend-only change (`edgeClient.ts`, `supabaseClient.ts`) — no Edge Function redeploy needed, just the next Vercel build off `master`. Not yet re-verified against the live site post-deploy; next person to touch this should confirm the browser console is clean on `/playdates` and an announcement detail page once Vercel picks up the merge.
