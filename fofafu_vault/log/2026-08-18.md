- 10:23 #team/eng/frontend [[features/reply-coach]] — CoachChip composer integration: `frontend/src/api/coach.ts` + `CoachChip/{CoachChip.tsx,useCoach.ts,index.ts}` wired into `CommentForm.tsx`, targeting the `coach` Supabase Edge Function (backend migrated off Express mid-flight; corrected the feature file's stale contract references and cross-linked [[features/migrate-render-to-vercel-supabase]], which hadn't documented this either); `threadContext` skipped for v1 (zero behavioral difference against the mock, avoids prop-drilling for nothing observable); Accept pill uses `bg-brand-primary-pressed` per the existing `brand-contrast.test.ts` static scan; two real bugs caught and fixed before commit — a superseded-request abort was clearing an already-visible suggestion (flicker), and an `instanceof DOMException` check failed against MSW's own AbortError class (fixed to check `.name` instead). Frontend 156/156 (+18 new), `tsc --noEmit` clean, no regressions. `frontend/e2e/composer.spec.ts` written but not run — no `.env` in this worktree, and the spec depends on `REPLY_COACH_ENABLED=true` being set as a live Supabase secret, unverifiable from here. Engineering kanban Backlog -> In Progress -> Review (`eng-frontend-20`).
