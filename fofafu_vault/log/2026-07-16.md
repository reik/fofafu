- 00:00 #team/eng [[features/community-playdate-badge]] — feature scaffolded directly (owner=engineering, priority=P2, source=user request to surface [[features/playdates]] availability in the Home dashboard's Community sidebar); mock approved by user before implementation
- 00:00 #team/eng/frontend [[features/community-playdate-badge]] — implemented: `supabase/functions/community/index.ts` returns `city`/`state`/`nextFreeSlotId` per family row (nextFreeSlotId via per-row future-free-slot lookup against `availability_slots`); `frontend/src/api/community.ts` extended with `CommunityFamilyDTO`; `Home.tsx` Community row shows City, State + "🗓 Playdate" badge (stretched-link pattern to avoid nested `<a>`s), name truncates past 24 characters only; `FamilyView.tsx` opens the existing `RequestPlaydateModal` automatically via a new `?requestSlot=<id>` query param. Frontend 134/134, tsc clean. Engineering kanban Backlog -> Review (solo implementation, no separate QA/code-review pass run yet).
