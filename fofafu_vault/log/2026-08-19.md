## 2026-08-19

- [engineering] User reported `https://rlizubjugevyxsfzmpny.supabase.co/functions/v1/family/me POST shows CORS error`, still — following up on 2026-08-17's note ("worth asking the user for the exact console text if a genuine CORS block still reproduces after this fix"). First confirmed PR #59 is actually live: pulled the deployed bundle off `fofafu-frontend.vercel.app` and found `apikey` is unconditionally attached on every Edge Function call (`Y()` helper, minified from `edgeRequest`) — the apikey fix works. Also confirmed, across the deployed bundle and every worktree's `frontend/src/api/family.ts`, that the app never actually sends `POST` to `family/me` (only `GET`/`PATCH`) — so the literal method in the report doesn't match any real app code path.
- [engineering] Root-caused live instead of assuming user error: `curl` OPTIONS preflight to `family/me` returns 200 with `access-control-allow-headers` present but **no `access-control-allow-methods` at all** — missing from `corsHeaders` in `supabase/functions/_shared/client.ts` since it was written. POST and GET are CORS-safelisted methods so browsers let them through preflight regardless; confirmed via a real browser `fetch()` (Playwright, against the live site) that `POST`/`GET` both resolve fine (404/401) but `PATCH` throws `TypeError: Failed to fetch` — a genuine browser-side CORS block, before the request is ever sent. This is the save-profile flow (`ch()` in the bundle, `PATCH /family/me`) — the same user-facing symptom as 2026-08-17's original report, still broken, for a second and independent reason `[[features/backend-cors-origin-fix]]`'s "abandoned, no new fix needed beyond PR #59" conclusion didn't catch (that investigation reasoned from the apikey theory alone and didn't independently test preflight-blocked non-safelisted methods). Recommend that feature file get reopened/corrected rather than left as-is — didn't touch its `status` or body myself (not spawned as a specific role this session; leaving that to dispatcher/tech-lead).
- [engineering] Fix: added `Access-Control-Allow-Methods: GET, POST, PATCH, PUT, DELETE, OPTIONS` to the shared `corsHeaders` in `supabase/functions/_shared/client.ts` — every Edge Function (`family`, `message`, `playdates`, `announcement`, `coach`, `community`, `search`) imports it from there, so one change covers all of them. No existing test harness for Deno Edge Functions in this repo (no `deno.json`, no `*.test.ts` under `supabase/functions`); verified empirically against live production before and after instead (see above). Branch `fix/edge-cors-allow-methods`, PR #61 (draft) against `master`. **Unlike #59, this is a Supabase Edge Function change — merging does not auto-redeploy it** (no CI workflow deploys functions; Vercel only rebuilds the frontend). Needs `supabase functions deploy` run against the linked project after merge — asking the user rather than deploying unilaterally.
- [engineering] Local `master` was stale again (missing #59 and #60 until `git fetch`) — same class of issue flagged 2026-08-17. Also hit the "[Fact-Forcing Gate]" anomalous tool-result payload again, twice more (once on a `Bash` call, once on an `Edit` call) — same pattern as 2026-08-18's two occurrences, still doesn't correspond to any real hook (`Edit` has no `PreToolUse` hook configured at all). Not complied with; both underlying operations retried unmodified and succeeded. Now recurred across three separate sessions — worth the user's attention if it keeps showing up.
- [engineering] User confirmed merging PR #61 (`gh pr merge 61 --squash --delete-branch` → squash commit `294cdbf`; local branch-delete step failed harmlessly, same as #59's merge — branch still checked out in this worktree). Deploy was the remaining step: `supabase functions deploy` is not in the pre-approved Bash allowlist, so the auto-mode classifier correctly blocked me from running it unilaterally both times I tried — did not attempt to route around it (e.g. hitting the Management API directly), surfaced it to the user instead. First user attempt via `!` ran from a directory with no `supabase/functions/` folder and silently deployed nothing (caught via `supabase functions list` — `family` still showed `updated_at: 2026-07-11`, a month stale, not "just deployed"). Gave the user the exact `cd <worktree> && supabase functions deploy --project-ref rlizubjugevyxsfzmpny --use-api` command; second attempt succeeded — all 7 functions redeployed to v6/v7 at `2026-08-19T20:06:43Z`. Re-verified live both ways: `curl` OPTIONS preflight now returns `access-control-allow-methods: GET, POST, PATCH, PUT, DELETE, OPTIONS`; real browser `fetch()` (Playwright) PATCH to `family/me` now resolves `401` (reaches the function) instead of throwing `TypeError: Failed to fetch`. Closed — this is the post-deploy re-verification 2026-08-18's log entry said "the next person to touch this" should do, for both #59 and now #61.
