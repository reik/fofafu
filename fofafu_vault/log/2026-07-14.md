## 2026-07-14

- [engineering] eng-infra-8 / seed-dummy.ts: added family thumbnail avatars (dicebear) and photo attachments on some feed posts (picsum) at the user's request, mirroring the placeholder-art pattern already in `seed-prod-sample-data.ts`. Backfills onto the 4 already-seeded families, not just future creates. Verified live against `rlizubjugevyxsfzmpny`: all 4 families have `avatar_url` set, 25 posts have `media_url`, real (non-dummy) accounts correctly untouched. Caught a real idempotency bug during live verification — the photo-backfill query filtered to only posts still missing `media_url` and indexed every-3rd within that shrinking set, so reruns kept adding more photos instead of converging (3 added, then 2 more on the very next run). Fixed by indexing every-3rd position over the full, stable post list instead. Confirmed stable across 3 consecutive runs after the fix. PR #54 (draft) updated.
- 14:17 #team/dispatch [[features/staging-environment]] — feature scaffolded (owner=engineering, status=drafting)
- 15:37 #team/dispatch [[features/supabase-rls-sensitive-columns]] — feature scaffolded (owner=engineering, status=drafting, priority=P0, source=Supabase Security Advisor: sensitive_columns_exposed)
- 15:39 #team/dispatch [[features/supabase-rls-sensitive-columns]] — routed to engineering; spawning backend-dev + qa-engineer in parallel
- 15:50 #team/eng/qa [[features/supabase-rls-sensitive-columns]] — test plan: SQL introspection (no live Advisor access) across 8 public tables; existing migrations consistent (RLS on, no blanket-true on sensitive data) but flagged table/column unidentified pending backend-dev migration — conditional pass, flagged gap
- 15:52 #team/eng/backend [[features/supabase-rls-sensitive-columns]] — static audit of all 6 migrations found no password/token columns in public schema (Supabase Auth owns credentials), but 5 tables' `USING (true)` SELECT policies lacked `TO authenticated`, exposing families.kid_count/city/state (foster-family PII) to the anon role; shipped 20260714000000_restrict_pii_to_authenticated.sql restricting SELECT to authenticated + REVOKE anon on 8 tables; flagged unconfirmed-table uncertainty in Open questions
- 16:05 #team/eng [[features/supabase-rls-sensitive-columns]] — tech-lead aggregated: reconciled qa-engineer's stale pre-migration "FAIL" framing against the now-landed migration (backend/QA evidence agrees, only QA's summary sentence predated the fix); filled E2E coverage as "No E2E coverage — backend-only"; two ACs (exact flagged table, live Advisor re-run) remain open pending human Supabase dashboard/CLI access, documented not buried; engineering kanban Backlog -> Review
- 16:05 #team/dispatch [[features/supabase-rls-sensitive-columns]] — engineering-lead returned success; status building -> review; company kanban In Progress -> Review. 2 ACs (exact flagged table, live Advisor re-run) require human follow-up with live Supabase dashboard/CLI access — surfaced in Open Questions, not silently dropped.
