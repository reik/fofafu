- 07:44 #team/eng [[features/ci-pipeline]] — first CI run on PR #8 went red: (1) typecheck failed on backend/tests/coach.test.ts (pre-existing brace bug, already fixed on master by #10 — resolved by merging master into feat/ci-pipeline); (2) backend `npm test` failed with "Could not find tests/**/*.test.ts" — `**` requires bash `globstar`, which GH Actions' non-interactive `/bin/bash -e` doesn't enable, so the glob matched nothing under the flat `backend/tests/` layout. Fixed by changing `backend/package.json` `test`/`test:run` scripts to `tests/*.test.ts` (matches the actual flat layout, shell-portable). Verified locally: 99/99 backend tests pass. Pushing as a follow-up commit on feat/ci-pipeline.
- 13:10 #team/eng #org-chart — added `e2e-test-writer` to the engineering line: org chart + writer-ownership in CLAUDE.md, new role file `.claude/agents/engineering/e2e-test-writer.md`, tech-lead audit checklist, `_template.md` `### E2E coverage` placeholder, and an engineering-charter decomposition heuristic (spawned alongside qa-engineer whenever frontend-dev's section is non-empty; writes/extends `frontend/e2e/<slug>.spec.ts` per the Playwright setup from #16; backend-only features get "No E2E coverage"). Activates on the next /dispatch after merge — no feature in flight is affected.
- 14:40 #team/eng/e2e [[features/feed-pages]] [[features/profile-pages]] [[features/community-search]] [[features/messages-pages]] — 4 Playwright specs added covering existing shipped flows: `frontend/e2e/feed-pages.spec.ts` (feed + compose), `frontend/e2e/profile-pages.spec.ts` (edit bio, "Message this family" link), `frontend/e2e/community-search.spec.ts` (validation + search result), `frontend/e2e/messages-pages.spec.ts` (empty state + DM flow), plus a shared `frontend/e2e/utils/login.ts` UI-login helper using the seeded dummy families. Extended `frontend/playwright.config.ts` to also boot the backend (`npm run e2e:setup` = migrate + `seed-dummy.ts`) against a dedicated `backend/e2e.db` on port 4100, with the frontend on port 5273 (`vite.config.ts` proxy made configurable via `E2E_BACKEND_PORT`) so e2e runs never collide with a developer's port-4000/5173 dev servers/db. 9/9 specs pass, twice in a row (idempotent against the persisted e2e.db).
- 20:42 #team/dispatch [[features/feed-virtualization]] — feature scaffolded (owner=engineering, status=drafting)
- 20:48 #team/dispatch [[features/feed-virtualization]] — routed to engineering; status drafting -> building; spawning frontend-dev, qa-engineer, e2e-test-writer in parallel
- 20:55 #team/eng/qa [[features/feed-virtualization]] — added frontend/src/pages/Feed.test.tsx (7 tests) covering loading/error/empty states, newest-first ordering, composer, "Load older posts" pagination, and variable-height card rendering against the new @tanstack/react-virtual windowed list (with ResizeObserver/getBoundingClientRect jsdom polyfills); 7/7 pass; full frontend suite 79/79 pass (22 files); tsc --noEmit clean
- 21:05 #team/eng/frontend [[features/feed-virtualization]] — rewrote frontend/src/pages/Feed.tsx to render the announcements list via @tanstack/react-virtual's useWindowVirtualizer (window-scroll model, since Layout has no own scroll container), with measureElement for variable-height cards; added @tanstack/react-virtual ^3.14.2 to frontend/package.json + lockfile via npm install; switched the feed's in-memory list from per-page replace to append-on-load-older (de-duped by id, newest-first preserved) so the windowed list has a growing dataset to virtualize, while keeping the cursor/useQuery/"Load older posts" button mechanics unchanged; added data-testid="feed-virtual-list" (role="feed") and data-testid="feed-virtual-item" hooks; tsc --noEmit clean, npm run build clean, full vitest suite 79/79 pass (22 files)
- 21:15 #team/eng/e2e [[features/feed-virtualization]] — extended frontend/e2e/feed-pages.spec.ts from 1 to 6 Playwright tests against the merged virtualized Feed.tsx (@tanstack/react-virtual useWindowVirtualizer, feed-virtual-list/feed-virtual-item test hooks): feed renders + compose (existing), newest-first ordering for new posts, reaction toggle, virtualization actually mounts only visible+overscan items (count > 0 and < 20 of a 20-item page), "Load older posts" accumulates (virtualizer total height grows), and scrolling mounts later cards without going blank. 6/6 pass x3 consecutive runs; full `npx playwright test` 14/14 pass; tsc --noEmit clean.
- 21:30 #team/eng/cr [[features/feed-virtualization]] — code review: 2 must-fix, 4 nice-to-have; master...HEAD. useWindowVirtualizer integration (measureElement, scrollMargin, getTotalSize, getItemKey, noUncheckedIndexedAccess guard) is correct. Accumulate-vs-replace pagination interpretation endorsed as the right reading of the AC (not scope creep), but the accumulated `items` state desyncs from React Query's cache after the composer's `invalidateQueries({queryKey: feedKeys.page})` — either the new post never appears (composing while paginated past page 1) or previously-accumulated older pages get silently dropped (composing while back on page 1). Flagged as blocking for tech-lead. Also flagged: Feed.test.tsx's "variable-height cards" test doesn't actually verify differential row measurement (fixed 1000x1000 getBoundingClientRect polyfill masks it) despite its AC-mapping claim.
- 21:50 #team/eng [[features/feed-virtualization]] — tech-lead aggregated; treated code review's must-fix #1 (accumulated `items` desyncs from React Query cache after composer invalidation) as blocking per the engineering quality bar (silent drop/discard of posts is a user-visible regression, not a fast-follow) and fixed it directly in frontend/src/pages/Feed.tsx: replaced useState+useEffect-append with a useMemo deriving `items` from all cached feedKeys.page queries (qc.getQueriesData), merged/de-duped/sorted newest-first, recomputed via a query-cache subscription scoped to success/error events on feedKeys.page. Also fixed the cascading Feed.test.tsx "renders the composer above the feed" failure (ambiguous /post/i button match) and addressed must-fix #2 (softened the "variable-height cards" test docstring to disclaim AC #3/overlap-clipping coverage, per the jsdom-limitations framing already in ### Test plan). Re-ran gates: tsc clean, build clean (397.16 kB/118.31 kB gzip), vitest 79/79 (22 files), playwright 14/14. engineering kanban In Progress -> Review.
- 21:55 #team/dispatch [[features/feed-virtualization]] — engineering returned status: success, requested_status: review; status building -> review; company kanban In Progress -> Review
