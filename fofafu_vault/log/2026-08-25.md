- 11:54 #team/eng/infra [[features/admin-access]] — deployed to the live Supabase project (rlizubjugevyxsfzmpny) at user request, outside the formal /dispatch flow: `supabase db push` applied the pending `20260823000000_admin_access.sql` migration (confirmed via `supabase migration list` first — it was the only pending migration; `is_admin()`, RLS policies, `admin_audit_log`, and the non-admin-readonly-column triggers are now live), then `supabase functions deploy admin` shipped the Edge Function (698 kB bundle). Verified `kurarei+5@gmail.com` already existed as a confirmed, non-banned Supabase Auth user (created 2026-07-13, predates this feature) — no signup/invite needed, closing this feature file's last unconfirmed Open Question. Smoke-tested the deployed function unauthenticated: `GET /functions/v1/admin/users` → 401 as expected. This closes the "Still not applied to the live Supabase project" caveat recorded in both kanban boards' Done entries for this feature — those lines are now stale; next dispatcher/tech-lead touch on this card should update them (not edited directly here per this vault's writer-ownership rule).
