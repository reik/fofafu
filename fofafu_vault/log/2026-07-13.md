## 2026-07-13

- [engineering] Root-caused and fixed the live `/login` infinite-redirect-loop bug reported against `fofafu-frontend.vercel.app`: (1) Vercel's `VITE_SUPABASE_URL` was pointed at an unrelated Supabase project (`yxeydnuydjuobgnfyour`, "reik_runfun") instead of the real `fofafu` project (`rlizubjugevyxsfzmpny`) — user corrected in Vercel dashboard, verified live via Playwright (auth calls now hit `rlizubjugevyxsfzmpny.supabase.co`, 401 on bad creds instead of hitting the wrong project). (2) `client.ts`'s 401 handler only cleared the local Zustand auth mirror, not the real Supabase session, so a 401 from the still-Render-hosted `messages`/`playdates`/`uploads`/`coach` routes (missing `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` on Render, now also set by user) let the session resurrect itself on reload and re-loop — fixed in [[features/migrate-render-to-vercel-supabase]] via PR #49 (`frontend/src/api/client.ts` now calls `supabase.auth.signOut()` before redirecting), merged to master (556f49a).
- [engineering] User confirmed intent to complete the full Render→Supabase migration (eng-infra-3/6/7/8). Starting eng-infra-6 (Edge Functions batch 2: message/playdates/coach) next, since it's what directly retires the Render dependency behind the bug above.
- [engineering] eng-infra-6/7 done: ported message/playdates/coach to Supabase Edge Functions and uploads to Storage (see [[features/migrate-render-to-vercel-supabase]] ### Backend for full detail); deployed and verified live against rlizubjugevyxsfzmpny. Caught and fixed a PostgREST filter-injection bug in message/index.ts via automated post-commit review, redeployed. Also fixed a pre-existing (and one newly-introduced) `ApiError`-vs-`EdgeApiError` mismatch across 9 components that was silently swallowing specific server error messages. Deleted `frontend/src/api/client.ts` as dead code (eng-infra-8 partial — every domain but coach is now off Express; e2e/playdates.spec.ts's Express-based seeding and the Render decommission itself remain). PR #50 opened as draft against master. eng-infra-3 downgraded to low-priority (no real prod data to migrate yet).
- [engineering] eng-infra-8 further along: `backend/scripts/seed-dummy.ts` was still writing to the dead local sqlite file (never repointed when the data layer moved to Postgres) — rewritten to use `supabaseAdmin()` (auth.admin.createUser + families/announcements inserts). `frontend/e2e/playdates.spec.ts` was seeding via a direct POST to the old Express backend on :4100, writing to a database the UI (on the `playdates` Edge Function since eng-infra-6) never reads — repointed at Supabase Auth REST + the deployed Edge Function. Removed now-dead pre-migration plumbing: `playwright.config.ts`'s Express+sqlite `webServer` entry, `vite.config.ts`'s `/api`/`/uploads` proxy, unused `VITE_API_URL`. Backend 147/147, frontend 132/132, both `tsc --noEmit` clean. PR #54 (draft). Found and flagged (not fixed — credentials): `backend/.env`'s `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` appear swapped/misnamed (`SUPABASE_URL` holds a secret-key-shaped value; the service-role key is under `SUPABASE_SECRET_KEY`), blocking a live run of the seed script and likely other Supabase-Auth-dependent paths until the user corrects it locally. Remaining on eng-infra-8: staging cutover confirmation, Express auth/route cleanup, Render decommission (deliberately still deferred as rollback).
- [engineering] eng-infra-8 verified live, real gap found: user fixed `backend/.env` (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) and provided the anon/publishable key for `frontend/.env`. Ran `seed:dummy` for real against `rlizubjugevyxsfzmpny` — creates the 4 dummy families correctly and is idempotent (verified by running twice). Ran the full e2e suite for real: 20-21/31 pass (login, AC1/AC2 playdates, most non-mutating specs). Fixed a real bug in the process — `playwright.config.ts` never loaded `dotenv`, so `playdates.spec.ts`'s Node-side Supabase calls (added this session) couldn't see `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` at all. The remaining ~10 failures (playdates AC3/4/5/6/7/8, profile bio edit, community search) are NOT flaky/concurrency — ran both parallel and `--workers=1`, same or worse failures serially — root cause is test-isolation architecture: these specs assumed a fresh `e2e.db` wiped every run, but now share one live, persistent Supabase project, so slots/requests/bio edits from one run pollute the next run's assertions. Flagging as a real follow-up decision (per-test cleanup, unique test data, or a dedicated Supabase branch/schema for e2e) rather than patching blind against live data under time pressure. PR #54 (draft) has the verified-working seed script + playdates data-seeding repoint; e2e-isolation is out of scope for this PR.
